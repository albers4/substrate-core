[package]
name = "substrate-core-example"
version.workspace = true
edition.workspace = true

[dependencies]
substrate-core-spec = { path = "../substrate-core-spec" }
substrate-core-impl = { path = "../substrate-core-impl" }
